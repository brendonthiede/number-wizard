# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
# Docstring coverage is a pre-merge check: below 80% the PR is blocked, not just warned.
reviews:
  pre_merge_checks:
    docstrings:
      mode: error
      threshold: 80
